import React from "react";
import styles from "./GameScreen.module.css";

export default (props: any) => {
    return (
        <div className={styles.GameScreen}>This is the main game screen!</div>
    );
};
