import React from "react";
import ScreenProps from "./ScreenProps";

import credits from "../../credits.json";

export default (props: ScreenProps) => {
    return <div>THIS IS THE CREDITS SCREEN</div>;
};
