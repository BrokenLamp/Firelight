[package]
name = "core"
version = "0.1.0"
authors = ["Broken Lamp"]
license = "MIT"
build = "build.rs"
exclude = ["artifacts.json", "index.node"]

[lib]
name = "core"
crate-type = ["cdylib"]

[build-dependencies]
neon-build = "0.3.3"

[dependencies]
neon = "0.10.1"
rodio = "0.10.0"
